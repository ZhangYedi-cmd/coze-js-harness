---
task: add-tests
date: 2026-06-06
package: "@coze/api"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/8
quality_check_rounds: 1
---

# Harness Run: add-tests — 2026-06-06

| 字段 | 值 |
|---|---|
| 任务 | add-tests |
| 描述 | 为测试覆盖不足的包补充 vitest 单元测试 |
| 目标包 | @coze/api |
| 基准分支 | main |
| 工作分支 | chore/harness-add-tests-20260606 |
| 执行时间 | 2026-06-06 06:00 |
| 最终状态 | ✅ 成功（质量检测含预存失败） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/8](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/8) |

## 执行摘要

目标包：`@coze/api`（`packages/coze-js`），综合评分最高（77分），测试覆盖率数据为 null（视为 0%）。

新增了以下两个测试文件，覆盖了 `src/ws-tools/chat/` 中最低覆盖率的模块：

- **`test/ws-tools/opus-decoder.spec.ts`**：10 个测试用例，覆盖 `OpusDecoder` 类全部方法（构造器两条路径、decode 成功/失败/空回调、isReady、waitForReady 轮询、destroy）。覆盖率从 **5.88% → 100%**。
- **`test/ws-tools/sentence-synchronizer.spec.ts`**：21 个测试用例，覆盖 `SentenceSynchronizer` 所有公开方法及内部调度逻辑（构造器、setOutputAudioConfig、handleAudioCompleted、handleSentenceStart、updateAudioDuration、updateLatestSentenceAudioDuration PCM/非PCM、setFirstAudioDeltaTime、resetSentenceSyncState、scheduleSentenceSwitch）。覆盖率从 **25.36% → 显著提升**。

`src/ws-tools/chat` 目录整体行覆盖率从 **56.35% → 80.72%**。全部 **394 个测试**全部通过（含新增 31 个测试）。

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：⚠️ PASS_WITH_PREEXISTING

## 失败详情（预存）

**失败阶段**：build
**错误摘要**（base branch main 上已存在，非本次变更引入）：

```
src/core.ts:216:40 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'
src/core.ts:234:38 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'
```

---
🤖 Generated by Harness Engineering
