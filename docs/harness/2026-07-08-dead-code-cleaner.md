---
task: dead-code-cleaner
date: 2026-07-08
package: "@coze/api"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/33
quality_check_rounds: 1
---

# Harness Run: dead-code-cleaner — 2026-07-08

| 字段 | 值 |
|---|---|
| 任务 | dead-code-cleaner |
| 描述 | 找出候选包，删除无任何引用的死代码文件 |
| 目标包 | @coze/api (packages/coze-js) |
| 基准分支 | main |
| 工作分支 | chore/harness-dead-code-cleaner-20260708 |
| 执行时间 | 2026-07-08 05:30 |
| 最终状态 | ⚠️ 成功（预存失败，与本次改动无关） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/33](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/33) |

## 执行摘要

通过 repo-lens 分析仓库候选包（综合评分：源码行数 + 内部依赖数 + 近期活跃度），选中 `@coze/api`（packages/coze-js，score 17，0 个内部依赖引用，近 30 天 0 次提交）作为目标包。history 记录显示该任务近 30 天内未处理过任何包，无排除项。

在 `packages/coze-js` 下生成临时 knip 配置（entry: `src/index.ts`、`src/ws-tools/index.ts`），运行 `npx knip --include files` 检测出 1 个未被引用的文件：

- `src/ws-tools/wavtools/lib/codecs/index.ts` — 仅 `export * from './g711'` 的 barrel 文件；实际代码中直接从 `./codecs/g711` 导入（`wav_stream_player.ts:3`），未通过该 barrel 引用，且全仓无任何文件 import `./codecs` 目录本身。排除法确认非 test fixture、非 .d.ts、非动态路径引用，确认为死代码，已删除。

删除后运行 `rush build --to @coze/api` 与 `rush lint --to @coze/api` 验证；lint 通过，build 因 `src/core.ts` 中预存的 `AxiosHeaders` 类型错误失败，与本次删除无关（详见下方失败详情）。

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：⚠️ PASS_WITH_PREEXISTING（build 阶段预存失败，lint / test:cov 均通过）

## 失败详情

**失败阶段**：build（DTS 生成阶段）
**错误摘要**：

```
packages/coze-js/src/core.ts:215:40 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
packages/coze-js/src/core.ts:233:40 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
```

**验证方式**：`git stash` 后在 `main` 分支上重新运行 `rush build --to @coze/api`，同样的 `core.ts` 类型错误依旧出现，确认为预存失败（PRE_EXISTING），与本次删除的 `codecs/index.ts` 无关。

---
🤖 Generated by Harness Engineering
