---
task: fix-ts-expect-error
date: 2026-06-12
package: "@coze/chat-sdk"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/10
quality_check_rounds: 1
---

# Harness Run: fix-ts-expect-error — 2026-06-12

| 字段 | 值 |
|---|---|
| 任务 | fix-ts-expect-error |
| 描述 | 删除 @ts-expect-error 注释，修复底层类型结构 |
| 目标包 | @coze/chat-sdk |
| 基准分支 | main |
| 工作分支 | chore/harness-fix-ts-expect-error-20260612 |
| 执行时间 | 2026-06-12 06:51 |
| 最终状态 | ✅ 成功 |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/10](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/10) |

## 执行摘要

在 `packages/chat-sdk/src` 中扫描到 14 处 `@ts-expect-error` 注释，分布在 9 个源文件中，全部修复：

**新增文件**：
- `src/libs/types/coze-api-augmentation.d.ts` — 模块声明增强，为 `@coze/api` 的 `CreateConversationReq` 和 `RetrieveBotReq` 补充 `connector_id?: string` 字段（已安装的 npm 版本缺失此字段）

**修复明细**（共 14 处）：

| 文件 | 行数 | 原因 | 修复方式 |
|------|------|------|---------|
| `chatflow/helper/chat-service.ts` | 218 | `connector_id` 不在 API 类型中 | 类型增强后直接使用 |
| `libs/services/helper/chat-service.ts` | 35, 50 | 同上（conversations.create / bots.retrieve） | 类型增强后直接使用 |
| `libs/services/helper/chat-service.ts` | 146 | `audio.transcriptions(...)` 调用方式错误 | 改为 `.create()` 方法调用 |
| `libs/utils/mini-coze-api.ts` | 248 | 将 `Transcriptions` 实例替换为函数 | 改为 override `.create` 方法 |
| `libs/utils/recorder-manager/h5-recorder-manager.ts` | 102 | `window.webkitAudioContext` 非标准属性 | 类型交叉声明 + guard 替换非空断言 |
| `libs/utils/recorder-manager/h5-recorder-manager.ts` | 109 | `createJavaScriptNode` 非标准属性 | 类型交叉声明 |
| `libs/ui-kit/atomic/upload/index.tsx` | 74 | `fileType/mediaType` 不在 Taro 类型中 | 类型交叉 |
| `libs/ui-kit/atomic/md-stream/helper/traversal-last-node.ts` | 20 | `RootContent[]` splice 接受 `RootContentLocal[]` | cast children 为 `RootContentLocal[]` |
| `libs/services/helper/message/send-message/multi-send-message.ts` | 162, 164 | `ChooseFileInfo.file` 联合类型无 `.name/.size` | cast 为 `File` 后访问 |
| `libs/services/helper/message/send-message/async-send-message.ts` | 76, 81 | `safeJSONParse` 返回 `unknown`；`data.execute_id` 缺失 | 添加类型断言 |
| `libs/services/helper/message/send-message/raw-send-message.ts` | 171 | `EnterMessage` 无 `isAudioTranslatingToText` | cast 为 `Partial<ChatMessage>` |

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：✅ 通过

| 检测项 | 结果 |
|--------|------|
| build（incremental） | ✅ PASS |
| lint（incremental） | ✅ PASS（首轮 4 个 lint 错误，自动修复后通过） |
| test:cov（incremental） | ✅ PASS |

## 质量检测修复记录

🔧 [#1] 修复 lint：consistent-type-assertions — 新增 `coze-api-augmentation.d.ts` 模块增强，替换 3 处 `as Parameters<...>` 类型断言

🔧 [#1] 修复 lint：no-non-null-assertion — 将 `AudioContextClass!` 替换为显式 guard（`if (!AudioContextClass) throw`）

---
🤖 Generated by Harness Engineering
