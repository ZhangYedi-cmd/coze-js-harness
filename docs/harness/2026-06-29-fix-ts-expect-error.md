---
task: fix-ts-expect-error
date: 2026-06-29
package: "@coze/api"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/24
quality_check_rounds: 2
---

# Harness Run: fix-ts-expect-error — 2026-06-29

| 字段 | 值 |
|---|---|
| 任务 | fix-ts-expect-error |
| 描述 | 删除 @ts-expect-error 注释，修复底层类型结构 |
| 目标包 | @coze/api |
| 基准分支 | main |
| 工作分支 | chore/harness-fix-ts-expect-error-20260629 |
| 执行时间 | 2026-06-29 06:59 |
| 最终状态 | ⚠️ 预存失败（lint + test 通过，build 为预存错误） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/24](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/24) |

## 执行摘要

目标包 `@coze/api`（packages/coze-js），共扫描到 12 个 `@ts-expect-error` 注释。

**已删除（8 个）**：
- `src/ws-tools/chat/opus-decoder.ts`（2 个）：`opus-encdec` 第三方库无类型声明 → 新增 `src/@types/opus-encdec.d.ts`，提供 OggOpusDecoder/OggOpusEncoder 完整类型
- `src/ws-tools/recorder/processor/opus-audio-processor.ts`（2 个）：同上；同时修复了 `encoder` 可能为 `undefined` 的类型错误（用局部变量替代非空断言）
- `test/auth.spec.ts`（2 个）：失效注释，`vi.stubGlobal` 不再需要抑制
- `test/version.spec.ts`（2 个）：失效注释，`global.process` 赋值改用类型断言 `as NodeJS.Process`

**保留（6 个）**：
- `test/version.spec.ts` 中 6 个注释仍抑制真实错误（全局 `uni`、`process.platform`、`navigator` 的测试 mock 赋值），保留

**新增文件**：
- `packages/coze-js/src/@types/opus-encdec.d.ts`：为 opus-encdec 全部 4 个导入路径提供 TypeScript 类型声明

## 质量检测结果

- 检测轮次：2 轮
- 最终状态：⚠️ 预存失败（PASS_WITH_PREEXISTING）

## 质量检测修复记录

- 第 1 轮：lint 报 `@typescript-eslint/no-non-null-assertion`（`this.encoder!`）→ 改用局部变量 `const encoder = new OggOpusEncoder(...)` 避免非空断言

## 预存失败详情

**失败阶段**：build（DTS 生成）
**错误摘要**：

```
src/core.ts(215,40): error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
src/core.ts(233,38): error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
```

已确认 main 分支上同一错误存在，与本次改动无关。

---
🤖 Generated by Harness Engineering
