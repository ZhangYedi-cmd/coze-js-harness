---
task: dep-cleaner
date: 2026-07-07
package: "@coze-infra/eslint-config"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/32
quality_check_rounds: 1
---

# Harness Run: dep-cleaner — 2026-07-07

| 字段 | 值 |
|---|---|
| 任务 | dep-cleaner |
| 描述 | 删除 package.json 中未被源码实际引用的冗余依赖 |
| 目标包 | @coze-infra/eslint-config (config/eslint-config) |
| 基准分支 | main |
| 工作分支 | chore/harness-dep-cleaner-20260707 |
| 执行时间 | 2026-07-07 06:16 |
| 最终状态 | ⚠️ 通过（含预存失败） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/32](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/32) |

## 执行摘要

通过 repo-lens 计算各包评分后，排除近 30 天内已被 dep-cleaner 处理过的包（`@coze/chat-sdk` PR #12、`@coze/api` PR #20 均在 30 天窗口内），从剩余候选中选定 `@coze-infra/eslint-config`（score=37.0，deps=52）。

对该包逐个核查 `dependencies` 的实际引用方式（`require`/`require.resolve`/`import`），排除 `package.json` 自身内容、peerDependencies、以及通过 `tsconfig.json extends`、ESLint `import/resolver` settings 等隐式方式引用的插件后，确认 14 个依赖未被实际使用：

- `@babel/preset-env`、`@babel/preset-react`
- `@eslint/eslintrc`、`@rushstack/eslint-config`
- `eslint-config-airbnb`、`eslint-config-prettier`、`eslint-config-react-app`
- `eslint-define-config`
- `eslint-plugin-babel`、`eslint-plugin-cypress`、`eslint-plugin-jsx-a11y`、`eslint-plugin-promise`、`eslint-plugin-redux-saga`
- `json5`

已从 `config/eslint-config/package.json` 中删除，并运行 `rush update` 同步 `pnpm-lock.yaml`（-856 行）。

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：⚠️ 通过（含预存失败）

| 阶段 | 结果 |
|---|---|
| build（增量，全仓库范围） | ⚠️ 预存失败：`@coze/api` 构建失败 |
| lint（增量，目标包） | ✅ PASS |
| test:cov（全量） | ✅ PASS |
| build（`rush build --to @coze-infra/eslint-config`） | ✅ PASS |
| lint（`rush lint --to @coze-infra/eslint-config`） | ✅ PASS |

## 失败详情

**失败阶段**：build（增量检测，非目标包本身）
**错误摘要**：

```
src/core.ts:215:40 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
src/core.ts:233:38 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
```

经 `git stash` 回退到 `main` 分支验证，`@coze/api` 上的同样两处错误在 base branch 上原样存在，与本次变更无关，确认为预存失败。目标包 `@coze-infra/eslint-config` 自身的 build/lint 均通过。

---
🤖 Generated by Harness Engineering
