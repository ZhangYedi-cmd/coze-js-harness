---
task: optimize-any-types
date: 2026-06-16
package: "@coze/api-web-example"
success: false
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/14
quality_check_rounds: 1
---

# Harness Run: optimize-any-types — 2026-06-16

| 字段 | 值 |
|---|---|
| 任务 | optimize-any-types |
| 描述 | 消除 TypeScript any 类型，补充精确类型声明 |
| 目标包 | @coze/api-web-example |
| 基准分支 | main |
| 工作分支 | chore/harness-optimize-any-types-20260616 |
| 执行时间 | 2026-06-16 |
| 最终状态 | ⚠️ 预存失败（Draft PR） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/14](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/14) |

## 执行摘要

在 `@coze/api-web-example` (examples/coze-js-web) 中消除了 **4 处显式 `any` 类型**（源代码文件中，排除 vendored .d.ts 文件），无新增 TypeScript 错误。

**修改文件：**
- `src/pages/chat/use-coze-api.ts`：导入 `EnterMessage` 和 `RoleType` 类型，将 `let messages: any[]` 改为 `let messages: EnterMessage[]`，消息对象中 `role: 'user'` 改为 `role: RoleType.User`，移除 eslint-disable 注释
- `src/pages/voice/index.tsx`：`handleSettingsSave` 参数 `values: any` → `Record<string, string>`；`handleFileUpload` 参数 `info: any` → `{ file: { originFileObj?: File } }`，移除两处 eslint-disable 注释
- `src/pages/chat-x/settings.tsx`：`handleSettingsSave` 参数 `values: any` → `Record<string, string>`，移除 eslint-disable 注释

**保留的 any 类型（vendored 文件，不修改）：**
- `src/lib/wavtools/dist/` 下所有 .d.ts 文件（第三方 wavtools 库的自动生成类型声明，共约 33 处 any）

## 质量检测结果

- 检测轮次：1 轮
- Lint：✅ 通过
- Test：✅ 通过（363 tests passed）
- Build：⚠️ 预存失败

## 失败详情

**失败阶段**：build（预存）
**错误摘要**：

```
packages/coze-js/src/core.ts:215,233 — TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
该错误在 main 分支上已存在，与本次改动无关。
```

---
🤖 Generated by Harness Engineering
