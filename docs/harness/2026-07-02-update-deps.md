---
task: update-deps
date: 2026-07-02
package:
success: false
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/27
quality_check_rounds: 1
---

# Harness Run: update-deps — 2026-07-02

| 字段 | 值 |
|---|---|
| 任务 | update-deps |
| 描述 | 升级 monorepo 中的非关键外部依赖到最新版本 |
| 目标包 | （全仓库） |
| 基准分支 | main |
| 工作分支 | chore/harness-update-deps-20260702 |
| 执行时间 | 2026-07-02 06:07 |
| 最终状态 | ⚠️ 预存失败（PASS_WITH_PREEXISTING） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/27](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/27) |

## 执行摘要

运行 `npx npm-check-updates --jsonUpgraded --deep` 获取全仓库可升级依赖列表，排除 react、react-dom、typescript、vite、webpack、rollup 及其插件生态、@microsoft/rush。按优先级（`@types/*` > eslint-*/prettier > lodash/dayjs > UI 组件库）选取 5 个候选包，全部为 `@types/*` 类型包在同一 major 版本内的升级：

| 包名 | 影响范围 | 旧版本 | 新版本 |
|------|----------|--------|--------|
| `@types/lodash` | infra/rush-x, examples/coze-js-web, examples/realtime-quickstart-react | `~4.17.13` | `~4.17.24` |
| `@types/mdast` | packages/chat-sdk | `4.0.3` | `4.0.4` |
| `@types/ws` | packages/coze-js | `^8.5.1` | `^8.18.1` |
| `@types/jsonwebtoken` | packages/coze-js, examples/coze-js-node | `^9.0.0` | `^9.0.10` |
| `@types/semver` | infra/rush-x | `^7.5.0` | `^7.7.1` |

`@types/node` 虽然 ncu 提示可升级到 `^26`，但当前 Node.js 运行时为 `v20.9.0`（`rush.json` 的 `nodeSupportedVersionRange` 为 `>=18`），跳过该升级以避免类型与实际运行时不匹配。

所有变更已通过 `node common/scripts/install-run-rush.js update` 重新生成 `pnpm-lock.yaml`。

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：⚠️ 预存失败（PASS_WITH_PREEXISTING）
- lint（受影响包：examples/coze-js-node, examples/coze-js-web, examples/realtime-quickstart-react, infra/rush-x, packages/chat-sdk, packages/coze-js）：✅ 通过

## 失败详情

**失败阶段**：build（增量检测，`@coze/api` / packages/coze-js）
**错误摘要**：

```
packages/coze-js/src/core.ts:233:38 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.
```

**预存验证**：通过 `git stash` 将本次依赖变更暂存后重跑同一增量 build 命令，`@coze/api` 依然以相同错误失败，确认该错误在 `main` 分支上已存在，与本次 `@types/*` 版本升级无关。

---
🤖 Generated by Harness Engineering
