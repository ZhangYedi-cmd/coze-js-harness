---
task: update-deps
date: 2026-07-04
package:
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/29
quality_check_rounds: 1
---

# Harness Run: update-deps — 2026-07-04

| 字段 | 值 |
|---|---|
| 任务 | update-deps |
| 描述 | 升级 monorepo 中的非关键外部依赖到最新版本 |
| 目标包 | （全仓库） |
| 基准分支 | main |
| 工作分支 | chore/harness-update-deps-20260704 |
| 执行时间 | 2026-07-04 05:54 |
| 最终状态 | ⚠️ 预存失败（PASS_WITH_PREEXISTING） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/29](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/29) |

## 执行摘要

运行 `npx npm-check-updates --deep --jsonUpgraded` 获取全仓库可升级依赖列表，排除 react、react-dom、typescript、vite、webpack、rollup 及其插件生态、@microsoft/rush，以及本次 ncu 提示的其他大版本跳跃（eslint 9→10、typescript 5→6、vitest 2→4、@types/node 20→26 等），避免与当前运行时（Node.js v20.9.0）及现有工具链产生兼容性风险。

选取 5 个候选包，均为 `@types/*` 类型包在同一 major 版本内的补丁/次版本升级，且与当前仍处于 Open 状态的 #27 PR 已升级的类型包（`@types/lodash`、`@types/mdast`、`@types/ws`、`@types/jsonwebtoken`、`@types/semver`）无重叠：

| 包名 | 影响范围 | 旧版本 | 新版本 |
|------|----------|--------|--------|
| `@types/webpack-env` | packages/chat-sdk, examples/coze-js-taro3 | `^1.13.6` | `^1.18.8` |
| `@types/express` | examples/coze-js-node | `^5.0.0` | `^5.0.6` |
| `@types/koa-bodyparser` | examples/quickstart-oauth-server | `^4.3.12` | `^4.3.13` |
| `@types/koa-router` | examples/quickstart-oauth-server | `^7.4.8` | `^7.4.9` |
| `@types/koa__cors` | examples/quickstart-oauth-server | `^5.0.0` | `^5.0.1` |

跳过的候选：`@types/uuid`（9→11，major）、`@types/koa`（2→3，major）、`@types/jest`（29→30，major）、`@types/shelljs`（0.8→0.10，0.x 范围视为破坏性变更）。

所有变更已通过 `node common/scripts/install-run-rush.js update` 重新生成 `pnpm-lock.yaml`。

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：⚠️ 预存失败（build / lint 阶段失败，已确认 base branch `main` 上同样失败，与本次依赖升级无关）

build（rush build）：❌ 预存失败 — `@coze/api`（packages/coze-js/src/core.ts:215/233，AxiosHeaders 类型上不存在 `includes`），经 `git stash` 验证 main 分支同样失败
lint（rush lint）：❌ 预存失败 — `@coze-arch/repo-lens`、`@coze/fact-check-extension`、`@coze/realtime-quickstart-vue`、`@coze/realtime-websocket`、`@coze/simult-extension`（ESLint flat config 兼容性问题、告警阈值等），经 `git stash` 验证 main 分支同样失败
test:cov（受影响包 @coze/chat-sdk）：✅ 通过

## 失败详情

**失败阶段**：build（`rush build`，`@coze/api`）
**错误摘要**：

```
packages/coze-js/src/core.ts:215:40 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.
packages/coze-js/src/core.ts:233:38 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.
```

**预存验证**：通过 `git stash` 将本次依赖变更暂存后重跑 `rush build --to @coze/api` 与 `rush lint`（对应失败包），错误在 `main` 分支上均以相同形式复现，确认与本次 `@types/*` 版本升级无关。

---
🤖 Generated by Harness Engineering
