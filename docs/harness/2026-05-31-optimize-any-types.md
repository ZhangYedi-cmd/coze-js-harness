---
task: optimize-any-types
date: 2026-05-31
package: "@coze/api"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/4
quality_check_rounds: 4
---

# Harness Run: optimize-any-types — 2026-05-31

| 字段 | 值 |
|---|---|
| 任务 | optimize-any-types |
| 描述 | 消除 TypeScript any 类型，补充精确类型声明 |
| 目标包 | @coze/api |
| 基准分支 | main |
| 工作分支 | chore/harness-optimize-any-types-20260531 |
| 执行时间 | 2026-05-31 05:01 |
| 最终状态 | ✅ 成功 |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/4](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/4) |

## 执行摘要

目标包 `@coze/api`（packages/coze-js）经 repo-lens 评分 24.6 选中，anyCount 76，近 30 天 0 commits。

在 16 个源文件中消除 / 收窄了 30 处 `any` 类型：

- `error.ts`：`rawError: any` → `rawError: unknown`
- `core.ts`：`WebSocket?: any` → `new(...args: unknown[]) => WebSocket`；`...msgs: any[]` → `unknown[]`
- `auth.ts`：`declare const uni: any` → 最小接口；`payload: Record<string, any>` → `Record<string, string | number>`
- `version.ts`：`declare const uni: any` → 完整 UniApp 系统信息接口（含所有访问字段）
- `utils.ts`：`declare const uni/chrome: any` → 最小接口；`isPlainObject/safeJsonParse` 参数 → `unknown`
- `fetcher.ts`：`handleError(error: any)` → `unknown` + `FetchError` 接口
- `ws-tools/chat/base.ts`、`ws-tools/recorder/pcm-recorder.ts`：`log/warn(...args: any[])` → `unknown[]`
- `ws-tools/recorder/processor/*.ts`：`declare const chrome: any` → 最小接口；`chrome.runtime.getURL` 改用可选链
- `ws-tools/utils/index.ts`：`declare const chrome: any` → 最小接口；内部变量 → `Record<string, unknown>`
- `ws-tools/wavtools/lib/local-loopback.ts`：`_debug/_error/_warn` → `unknown[]`
- `ws-tools/wavtools/lib/wav_recorder.ts`：`AudioTrackConfig` index → `unknown`；`_devices: any[]` → `MediaDeviceInfo[]`；chunkProcessor 返回 `any` → `void`
- `ws-tools/wavtools/lib/worklets/*.d.ts`：`any` → `string`
- `resources/websockets/types.ts`：`parameters?: Record<string, any>` → `Record<string, unknown>`

保留带 eslint-disable 的 `any`（跨平台必要性）：
- `file: File | any`（兼容 uni-app filePath 对象等平台特定文件类型）
- `adapterFetch(options: any)`（axios 内部 adapter 接口）
- `mergeConfig/setValueByPath` 泛型约束（caller 兼容性）

## 质量检测结果

- 检测轮次：4 轮
- 最终状态：✅ 通过

## 质量检测修复记录

- 🔧 [#1] 修复：buildWebsocketUrl 参数恢复 `any`（具名接口不满足 Record index signature 约束）
- 🔧 [#2] 修复：version.ts 扩展 uni getSystemInfoSync 返回类型；fetcher.ts adapterFetch 参数保留 `any`
- 🔧 [#3] 修复：non-null assertion → optional chaining（`chrome.runtime?.getURL?.()`）；`file` 类型恢复 `File | any`（下游 uni-app 需要 filePath 属性访问）
- 🔧 [#4] 修复：setValueByPath 泛型约束恢复；eventReceipts 恢复 `any`（_event 返回类型推断）

---
🤖 Generated by Harness Engineering
