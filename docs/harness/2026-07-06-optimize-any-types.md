---
task: optimize-any-types
date: 2026-07-06
package: "@coze/realtime-api-example"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/31
quality_check_rounds: 1
---

# Harness Run: optimize-any-types — 2026-07-06

| 字段 | 值 |
|---|---|
| 任务 | optimize-any-types |
| 描述 | 消除 TypeScript any 类型，补充精确类型声明 |
| 目标包 | @coze/realtime-api-example（examples/realtime-console） |
| 基准分支 | main |
| 工作分支 | chore/harness-optimize-any-types-20260706 |
| 执行时间 | 2026-07-06 06:44 |
| 最终状态 | ⚠️ 通过（含预存失败） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/31](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/31) |

## 执行摘要

通过 repo-lens 评分（loc=6126, internalDeps=3, recentCommits=0, anyCount=19）选中候选包。评分更高的 `@coze/api`、`@coze/api-web-example`、`@coze/uniapp-api` 均已有未合并的 optimize-any-types 草稿 PR（#30、#14、#13）在处理相同任务，为避免重复劳动改选下一顺位候选包 `@coze/realtime-api-example`。

扫描发现真实显式 `any` 用法（排除注释）共 11 处，分布在 5 个文件：

- `src/pages/main/index.tsx`：新增 `RealtimeEventPayload` 接口描述 WS 事件负载结构（`event_type` / 嵌套 `data.role` / `data.content` / `transcriptData` / `translationData`）。`EventData.data` 由 `any` 改为该类型；`handleAllMessage` 的回调参数按库导出的 `EventCallback` 真实签名改为 `unknown`，内部显式断言为 `RealtimeEventPayload`；`mergeEvent` 补全 `(prevEvents: EventData[], event: EventData): EventData | null` 签名，原先未做空值检查的 `data.data.xxx` 访问改为可选链 + 默认值。
- `src/pages/main/setting-form.tsx`：`loadData` 的 `setData`/`fetchData` 参数改为泛型 `<T extends { value: string }>`，由调用方（`WorkspaceOption`/`BotOption`/`VoiceOption`）具体化，避免用 `any[]` 抹平真实类型。
- `src/pages/main/comfort-strategy-form.tsx`：新增并导出 `ComfortStrategyValues`、`ComfortStrategyEventData` 接口，替换 `onSubmit` 回调的 `any` 参数。
- `src/pages/main/header.tsx`：`handleComfortStrategySubmit` 复用上述类型；新增 `ConversationChatRequiresActionEvent`、`LiveCreatedEvent` 接口描述 `client.on(...)` 回调实际负载结构，回调参数改为 `unknown` 后按接口断言。
- `src/hooks/use-network-error.tsx`：`NETWORK_QUALITY` 事件的 `(data as any).uplinkNetworkQuality` 改为具名断言 `data as { uplinkNetworkQuality: NetworkQuality }`，去掉 `eslint-disable` 注释。

修改后该包内 `grep -rn '\bany\b'` 无匹配，5 个改动文件的 `tsc --noEmit` 与 `eslint` 均通过。

## 质量检测结果

- 检测轮次：1 轮
- 增量 lint（5 个改动文件）：✅ 通过，0 error
- 增量 test:cov：✅ 通过（`@coze/realtime-api-example` 测试脚本为 `exit 0`；`@coze/api` 覆盖率正常，仅有与本次改动无关的端口占用告警）
- 增量 build：❌ 失败 → 经 `git stash` 对比验证，同一构建命令在未修改的 `main` 分支上产生完全相同的错误，判定为预存失败（PASS_WITH_PREEXISTING）
- 最终状态：⚠️ 通过（含预存失败）

## 失败详情

**失败阶段**：build（`@coze/api` 的 DTS 生成，阻塞下游 `@coze/realtime-api-example` / `@coze/realtime-api`）

**错误摘要**：

```
packages/coze-js/src/core.ts:215:40 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.
packages/coze-js/src/core.ts:233:38 - error TS2339: Property 'includes' does not exist on type 'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.
```

本次改动完全未涉及 `packages/coze-js`，`git stash` 后在 `main` 分支运行 `rush build --to @coze/api` 复现完全相同的两处错误（与开放中的 PR #30 报告一致），不在本任务范围内修复。

---
🤖 Generated by Harness Engineering
