---
task: dep-cleaner
date: 2026-06-25
package: "@coze/api"
success: false
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/20
quality_check_rounds: 1
---

# Harness Run: dep-cleaner — 2026-06-25

| 字段 | 值 |
|---|---|
| 任务 | dep-cleaner |
| 描述 | 删除 package.json 中未被源码实际引用的冗余依赖 |
| 目标包 | @coze/api |
| 基准分支 | main |
| 工作分支 | chore/harness-dep-cleaner-20260625 |
| 执行时间 | 2026-06-25 06:16 |
| 最终状态 | ⚠️ 预存失败（Draft PR） |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/20](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/20) |

## 执行摘要

目标包 **@coze/api** (`packages/coze-js`) 的 `devDependencies` 静态分析结果：

扫描 `src/` 下所有 `.ts` 文件的 `import` 语句，与 `package.json` 中声明的依赖对比，确认以下两个依赖未被实际引用：

1. **`@swc/core`** (`^1.3.14`) — 未在任何 source/config 文件中引用；`@rslib/core` 底层使用 Rspack，内置 Rust 原生 SWC 转换器，无需额外安装 Node.js 绑定
2. **`@types/whatwg-fetch`** (`^0.0.33`) — 未在任何 `tsconfig` 的 `types` 数组中引用；`tsconfig.json` 中已设置 `"lib": ["es2015", "dom"]`，DOM lib 原生包含 Fetch API 类型定义，本包冗余

已确认的保留依赖：
- `dependencies` 中所有 9 个包均在 `src/` 中有对应 `import`，全部保留
- `axios` 同时是 `peerDependencies`，devDependencies 中保留用于测试
- `@swc/core` 和 `@types/whatwg-fetch` 无任何引用，安全删除

## 质量检测结果

- 检测轮次：1 轮
- Lint：✅ 通过
- Test：✅ 通过
- Build：⚠️ 预存失败

## 失败详情

**失败阶段**：build（预存失败，与本次变更无关）

**错误摘要**：

```
src/core.ts:215:40 - error TS2339: Property 'includes' does not exist on type
  'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.

src/core.ts:233:38 - error TS2339: Property 'includes' does not exist on type
  'string | number | true | string[] | AxiosHeaders'.
  Property 'includes' does not exist on type 'number'.
```

已通过 `git stash` 回退到 main 分支验证，相同错误在 base branch 上已存在（PRE_EXISTING），确认非本次变更引入。

---
🤖 Generated by Harness Engineering
