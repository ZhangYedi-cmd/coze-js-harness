---
task: upgrade-rush-pnpm
date: 2026-07-03
package:
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/28
quality_check_rounds: 1
---

# Harness Run: upgrade-rush-pnpm — 2026-07-03

| 字段 | 值 |
|---|---|
| 任务 | upgrade-rush-pnpm |
| 描述 | 将 Rush 和 pnpm 升级到 npm 最新稳定版本 |
| 目标包 | （Repo 粒度，无单一目标包） |
| 基准分支 | main |
| 工作分支 | chore/harness-upgrade-rush-pnpm-20260703 |
| 执行时间 | 2026-07-03 06:01 |
| 最终状态 | ✅ 成功 |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/28](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/28) |

## 执行摘要

- Rush：`5.140.0` → `5.177.1`
- pnpm：`9.12.3` → `10.34.4`（未采用最新的 11.9.0，因其要求 Node.js ≥22.13，而当前构建环境为 Node.js 20.9.0；10.34.4 是与当前 Node 版本兼容的最新稳定版本）
- 运行 `node common/scripts/install-run-rush.js update --full` 完成全量依赖解析刷新
- 同步更新 `common/autoinstallers/rush-lint-staged/package.json` 中固定的 `@microsoft/rush-lib`（5.140.0 → 5.177.1），并通过 `rush update-autoinstaller --name rush-lint-staged` 重新生成其 `pnpm-lock.yaml`，避免版本不一致导致 pre-commit lint-staged 报错阻塞提交
- `--full` 刷新重新解析了部分未锁定精确版本的依赖（如 `typescript` 5.6.3→5.9.3、`@ant-design/x` 1.0.2→1.0.6），修复了由此暴露的少量真实类型/导入问题（详见下方修复记录）

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：✅ 通过（`rush build` 全绿；`rush test:cov` 全绿；`rush lint` 存在失败，但逐一与 `main` 基准分支比对错误签名完全一致，均为预存失败，与本次变更无关，判定为 PASS_WITH_PREEXISTING）

## 质量检测修复记录

`rush update --full` 将 `@coze/api` 构建链路中的 `typescript` 从 5.6.3 提升到 5.9.3，暴露出以下真实类型问题，逐一修复：

1. `packages/coze-js/src/core.ts:215,233` — `contentType` 联合类型不含 `.includes`，补充 `typeof contentType === 'string'` 收窄
2. `packages/coze-js/src/ws-tools/utils/index.ts` — `encodeG711U` 返回类型显式标注为 `Uint8Array<ArrayBuffer>`，避免类型被隐式加宽为 `ArrayBufferLike`
3. `packages/coze-js/src/ws-tools/wavtools/lib/analysis/audio_analysis.ts:59` — `analyser.getFloatFrequencyData(fftResult)` 调用处按新 DOM 类型收窄为 `Float32Array<ArrayBuffer>`
4. `packages/coze-js/src/ws-tools/wavtools/lib/wav_packer.ts` — `_packData` 返回类型标注为 `Uint8Array<ArrayBuffer>`；`new Blob(output, ...)` 调用处按 `BlobPart[]` 收窄

`@ant-design/x` 由 1.0.2 提升到 1.0.6 后，子路径导出由驼峰命名改为短横线命名：

5. `examples/coze-js-web/src/pages/chat-x/index.tsx` — `@ant-design/x/es/useXChat` → `@ant-design/x/es/use-x-chat`

## 预存失败说明

以下 9 个包的 `rush lint` 失败在 `main` 分支（未应用本次变更）上同样存在，属于历史遗留的 ESLint 9 flat config 与旧版工具链（`@vue/cli-plugin-eslint`、旧版 CLI flag 等）不兼容问题，与本次 Rush/pnpm 升级无关，未做处理：

`@coze-arch/repo-lens`、`@coze/api-web-example`、`@coze/fact-check-extension`、`@coze/realtime-api-example`、`@coze/realtime-call-up`、`@coze/realtime-quickstart-react`、`@coze/realtime-quickstart-vue`、`@coze/realtime-websocket`、`@coze/simult-extension`

---
🤖 Generated by Harness Engineering
