---
task: fix-eslint-disable
date: 2026-06-24
package: "@coze/api"
success: true
pr: https://github.com/ZhangYedi-cmd/coze-js-harness/pull/19
quality_check_rounds: 1
---

# Harness Run: fix-eslint-disable — 2026-06-24

| 字段 | 值 |
|---|---|
| 任务 | fix-eslint-disable |
| 描述 | 删除不必要的 eslint-disable 注释，修复根因 |
| 目标包 | @coze/api |
| 基准分支 | main |
| 工作分支 | chore/harness-fix-eslint-disable-20260624 |
| 执行时间 | 2026-06-24 06:14 |
| 最终状态 | ✅ 成功 |
| PR | [https://github.com/ZhangYedi-cmd/coze-js-harness/pull/19](https://github.com/ZhangYedi-cmd/coze-js-harness/pull/19) |

## 执行摘要

目标包：`@coze/api`（packages/coze-js），综合评分 53（基础分 15 + eslintDisableCount bonus 38）。
`@coze/chat-sdk` 因 11 天前被 fix-eslint-disable 处理过（< 30 天）已排除。

扫描发现 19 个 eslint-disable 注释，分类处理如下：

**已修复（9 处）**：
- `no-empty`（1 处）：`fetcher.ts` 空 catch 块改为 `catch { /* JSON parse failed */ }`
- `@typescript-eslint/no-explicit-any` - `file: File | any`（4 处）：`transcriptions.ts`、`features.ts`（×3）、`files.ts` 改为 `File | Blob | ArrayBuffer | string`
- `@typescript-eslint/no-explicit-any` - `adapterFetch`（1 处）：`fetcher.ts` 改用 `InternalAxiosRequestConfig` + 返回 `Promise<AxiosResponse>`
- `@typescript-eslint/no-explicit-any` - `buildWebsocketUrl`（1 处）：`utils.ts` 改用 `params?: object`
- `@typescript-eslint/no-explicit-any` - `setValueByPath`（1 处）：`ws-tools/utils/index.ts` 改为 `T extends object` + 内部 `as unknown` 转换

**保留（10 处）**：
- `max-params`（5 处）：白名单纯风格规则，按策略跳过
- `@typescript-eslint/no-namespace`（1 处）：`bots.ts` 中 `Bots.BotModeType` 是公开 API，移除会破坏兼容性
- `security/detect-object-injection`（1 处）：`utils.ts` `mergeConfig` 中动态 key 访问，需要复杂类型重构
- `@typescript-eslint/no-explicit-any`（2 处）：`mergeConfig`（调用方传入各种接口类型无法统一约束）、`eventReceipts`（AudioWorkletNode 消息负载类型运行时动态决定）
- `no-console`（1 处）：`wav_recorder.ts` 中是意图明确的调试警告

## 质量检测结果

- 检测轮次：1 轮
- 最终状态：✅ 通过

`rush lint` 全部通过。`npx tsc -p tsconfig.build.json --noEmit` 仅剩 2 个预存错误（`core.ts` 中 `AxiosHeaders.includes` 类型问题，与本次改动无关）。

---
🤖 Generated by Harness Engineering
